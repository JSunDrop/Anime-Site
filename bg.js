const c=document.getElementById('bg'),x=c.getContext('2d');function r(){c.width=innerWidth;c.height=innerHeight}addEventListener('resize',r);r();let s=Array.from({length:160},()=>({x:Math.random()*c.width,y:Math.random()*c.height,v:.1+Math.random()*.3}));(function t(){x.fillStyle='#0a1224';x.fillRect(0,0,c.width,c.height);x.fillStyle='rgba(124,198,255,.85)';for(const a of s){x.globalAlpha=.4+Math.sin((performance.now()/1e3+a.x)*a.v)*.3;x.fillRect(a.x,a.y,2,2);a.y+=a.v;if(a.y>c.height){a.y=-2;a.x=Math.random()*c.width}}requestAnimationFrame(t)})()